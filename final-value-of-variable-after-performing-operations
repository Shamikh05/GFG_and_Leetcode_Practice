class Solution {
public:
    int finalValueAfterOperations(vector<string>& operations) {
        
        int X = 0;
        
        for(string str : operations) str[1]=='+' ? X++ : X--;
        
        return X;
    }
};
